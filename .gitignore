# Local run artifacts
*.log

# Arturo test scratch files if a future test writes inside repo
tests/**/.tmp/
*.db

# Upstream skill is now committed for hot-start; update with:
# git -C skills/arturo-language pull --ff-only
#.arturo-skill/
#.tmp/