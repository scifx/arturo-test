# 实战项目反馈

## 当前状态

- 已建立 `word-frequency/` 作为第一个实战项目样例。
- 已建立 `vector-math/`：用 `import ./{vector}!` 拆出可复用矢量库，并覆盖错误路径与 JSON 报告。
- 项目级测试会被 `./tests/run-all.sh` 与模块测试一起运行。

## 后续建议

1. 增加 CLI 参数项目：验证 `args`、文件路径、错误处理。
2. 增加 HTTP JSON client 项目：验证 `request.get/post` 和 JSON API 模式。
3. 继续扩展 mini tool loader：在 `vector-math` 的本地 `import` 之上，验证动态路径与工具注册结构。
