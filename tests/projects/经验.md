# 实战项目经验

1. 项目示例不要只展示语法，要模拟 agent 常见任务：读写文件、处理 JSON、调用外部命令、组织函数。
2. 即使是项目，也要保留 `ensure.that:` 断言，保证能回归验证。
3. 项目内部可复用模块测试里的经验，但要记录组合时的新坑点。
4. 临时输出优先写 `/tmp`，避免污染仓库。
5. 多文件项目用 `import ./{name}!` 加载同目录模块；库文件用 `if standalone?` 包住自测，避免被导入时重复执行。
