# word-frequency 经验

1. 清洗文本建议分步骤写：

```arturo
cleaned: lower text
cleaned: replace cleaned "," ""
cleaned: replace cleaned "." ""
```

2. 词频 dictionary 的动态 key 模板：

```arturo
current: (key? freq w)? [get freq w] [0]
set freq w current + 1
```

3. 项目输出可以先构造 dictionary，再用 JSON 往返断言，兼顾可读性和机器可验证性。
