# arithmetic 反馈

## 已验证

- 矢量逐分量加减可直接用 `+` / `-`；缩放用 `*`。
- 归一化必须用 `//`（`fdiv`）。`/` 对整数是截断除法，`3 / 5` 为 `0`。

## 待验证

- 从上游 skill / runtime 查询本模块函数列表。
- 优先选择 agent 高频用法写最小可执行示例。
- 将失败、歧义或文档不足记录在这里，方便反馈给 skill 上游。
