# Modules 分区反馈

## 当前模块覆盖

第一批从官方库模块中选择 agent 高频场景：Core、Collections、Strings、Iterators、Files。

## 官方示例导入状态

- 所有 26 个官方模块目录均已填充 `官方示例.md`。
- 当前上游 skill/runtime 共导出 904 段 `info.get` 官方示例。
- 已整模块原样跑通并纳入 `run-all.sh` 的官方示例模块：
  - `arithmetic`
  - `bitwise`
  - `colors`
  - `comparison`
  - `crypto`
  - `dates`
  - `paths`
  - `quantities`
  - `sets`
  - `statistics`

## 暂不整模块原样执行的原因

部分模块的官方示例需要输入、网络、UI、数据库文件、外部动态库，或者包含当前 runtime/index 的已知差异。它们仍然保留在各自的 `官方示例.md` 中，后续应改写为 curated tests。

## 需要继续补齐

- Exceptions：`try`、错误结构、`inspect err`。
- System：`execute`、环境变量、进程调用。
- Network/Web：HTTP request 模式与 JSON API。
- Numbers/Math：右到左求值、无优先级、整数/浮点边界。
