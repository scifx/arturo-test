# paths 反馈

## 已验证

- 总章路径分量、URL 分量、`normalize`、`absolute?` 与 runtime 一致。

## 给 skill 的重点

- `extract.port` 的类型必须写成 `:string`。网页打印 `8080` 看起来像整数。
- 总章 Common Patterns 里的 `pathBuilder` / `validatePath` 是示例函数，不是 builtin。
