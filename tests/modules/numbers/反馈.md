# numbers 反馈

## 已验证

- `hypot 3 4` 返回 `5.0`，适合作为 2D 矢量模长。
- `sqrt` + 点积可用于 n-D 模长：`vecMag [1 2 2]` 为 `3.0`。
- `3 / 5` 是整数除法得到 `0`；单位矢量必须用 `3 // 5` 得到 `0.6`。

## 待验证

- 从上游 skill / runtime 查询本模块函数列表。
- 优先选择 agent 高频用法写最小可执行示例。
- 将失败、歧义或文档不足记录在这里，方便反馈给 skill 上游。
