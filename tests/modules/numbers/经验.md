# numbers 经验

本文件沉淀 agent 编写 `numbers` 模块 Arturo 代码时可复用的经验。

## 记录格式建议

1. 先写可运行代码片段。
2. 再写为什么这样写。
3. 标注是否来自官方 `info.get` example、skill 文档，还是本仓库实测。

## 已验证

- 2D 模长优先 `hypot x y`；更高维用 `sqrt` 点积。
- 需要小数结果时用 `//`，不要用 `/`。来源：本仓库 `tests/projects/vector-math` 实测。
