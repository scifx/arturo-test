# iterators 反馈

## 已验证

- 总章 map/select/filter/every?/some?/fold/gather/collect/cluster/chunk 与 runtime 一致（除 enumerate 注释）。

## 网页错误

- **`enumerate numbers 'x -> x > 5` 对 `[2 4 6 8]` 文档写 3，实测 2。**

## 给 skill 的重点

- `select` vs `filter` 方向相反，必须举例。
- 管道 `|` 总章有长例子，默认回归不要跑（输出长、依赖打印格式）。
