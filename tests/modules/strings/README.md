# Strings 测试说明

本分区验证文本处理与正则写法。

## 测试文件

| 文件 | 覆盖点 | 来源 |
|---|---|---|
| `text-and-regex.art` | `lower`、`upper`、`split.words`、`replace`、`match.once`、`{/.../}` 正则字面量 | 官方函数签名 + skill 已验证正则经验 |

## 运行

```bash
./tests/run-all.sh
```
