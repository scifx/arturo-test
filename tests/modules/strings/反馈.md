# strings 反馈

## 已验证

- 大小写、前后缀、strip、join、slice、`~` 插值、字面 replace、正则 replace 可用。
- **`upper?` 在当前 runtime 可以解析。** 旧报告里“未解析”已过时。

## 网页与 runtime 不符

- `match` 返回 `:block`。
- `truncate` 的省略号前有空格。

## 给 skill 的重点

- 正则必须是 `{/.../}`。
- `~` 是求值，不是纯替换。
- 不要再用 `upper?` 当已知 gap。
