# Strings 反馈

## 已验证

- `split.words` 能将字符串按空白切为 block。
- `replace string match replacement` 返回新字符串。
- 当前 bundled runtime 的正则字面量使用 `{/.../}`，`match.once` 返回匹配 block，例如 `["123"]`。

## 可反馈给 skill 的点

- 必须继续强调正则写法是 `{/.../}`，不要写成很多语言中常见的 `/.../`。
- `match.once` 的返回值不是裸字符串，而是 block；测试中应断言 `["123"]`。
