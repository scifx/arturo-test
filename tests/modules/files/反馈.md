# files 反馈

## 已验证

- 本地 write/read/append/exists?/file?/directory?/delete、`write.json`/`read.json` 可用。
- `write.json v null` 与 `write.json v ø` 相同。

## 给 skill 的重点

- 总章 URL `read` 不能进默认回归。
- `read` 对普通字符串可能不当文件读。需要文件时用 `read.file` 或先 `exists?`。
