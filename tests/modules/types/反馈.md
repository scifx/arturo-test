# types 反馈

## 已验证

- 基本 `type` / `integer?` / `is?` / `to` / `define` + `string:` 方法可运行。
- 构造必须 `to :person [...]!`，那个 `!` 不能省。

## 给 skill 的重点

- 总章 magic methods 大表只在网页上，CLI `info` 没有。
- `\field` ≡ `this\field` 应和 `module` 章节交叉引用。
- 继承示例（`define :dog is :animal`、`super`）尚未实测，skill 不要先当已验证。
