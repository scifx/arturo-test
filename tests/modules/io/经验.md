# io 经验

来源：https://arturo-lang.io/documentation/library/io + 实测。

- `print` 带换行；`prints` 不换行；`print.lines` 逐项换行。
- `color #red "Error!"` 返回 string。
- `terminal\width` / `terminal\height` 是 integer。
- `input` / `cursor` / `goto` / `clear` 不要进非交互回归。
