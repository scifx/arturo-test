# comparison 反馈

## 已验证

- 总章 equality / ordering / range / compare / same? 均与 runtime 一致。
- 牛顿单位例子 `1`N` 与 `1`kg.m/s2` 可运行。

## 给 skill 的重点

- 从 Python/JS 来的人会写 `<=`，这里是 `=<`。
- `<=>` 在总章里是 between，不是单独的三路比较；三路比较用 `compare`。
- `same?` vs `=` 是抽 skill 时的高价值点（quantity 换算）。
