# system 反馈

## 已验证

- `sys` / `process` / `env` / `path` / `superuser?` / `standalone?` 可查询。

## 网页与 runtime

- 总章打印示例是 macOS 主机名，不能当断言。
- **`sys\os` 是 `:literal`。** 写 `string? sys\os` 会失败。

## 给 skill 的重点

- 查系统字段先 `inspect` / `type`，不要猜 string。
- `execute.code` 才带退出码。
