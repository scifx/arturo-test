# reflection 反馈

## 已验证

- `arity`、`set?`、`symbols`、`info.get`、`standalone?` 可用。

## 给 skill 的重点

- 总章 `analyzeFunction` 里的 `info\\options` 实际字段名是 `attrs`（本仓库 core 测试已见过）。
- `info.get` 只吃直接字面量 `'split`。
