# exceptions 反馈

## 已验证

- `throw "message"` 可被 `throws? [ ... ]` 捕获，适合库函数的维度检查。
- `tests/projects/vector-math` 用它覆盖了长度不一致、非数值分量、零向量单位化和非 3D 叉积。

## 待验证

- 从上游 skill / runtime 查询本模块函数列表。
- 优先选择 agent 高频用法写最小可执行示例。
- 将失败、歧义或文档不足记录在这里，方便反馈给 skill 上游。
